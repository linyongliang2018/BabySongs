# 宝宝儿歌播放器 — 联调与运行说明

## 儿歌目录（任选）

应用不再固定「宝宝播放器」路径。首次请在 **专辑** 页点击 **「选择儿歌目录」**（或右上角「选择目录」），在系统文件界面中授权要扫描的文件夹（例如 **Download / 下载**、网盘同步目录等）。授权会持久保存，下次进入会自动扫描。

### 扫描规则

- **一级子文件夹**即一张专辑，专辑名 = 文件夹名。
- 若 **音频/视频文件直接放在所选根目录下**（未建子文件夹），会显示一张 **「本目录」**（或系统显示的文件夹名）专辑。
- 支持格式：常见音频（mp3、m4a、aac、flac、ogg、opus、wav 等）与常见视频容器（mp4、m4v、3gp、mkv、webm、mov；**仅播放音频轨**）；不含 WMA；文件名排序；标题为去掉扩展名的文件名。
- 空文件夹仍会显示为专辑，歌曲数为 0。

### 示例

- 选择 `Download` 目录：其下 `儿歌A/`、`儿歌B/` 为两张专辑；若 `Download` 根下直接有 `.mp3`，会多一张「本目录」专辑。
- 选择网盘目录：同上。

## 构建与安装

1. 使用 Android Studio 打开工程根目录。
2. 执行 **Run** 或命令行：`./gradlew assembleDebug`。
3. 将生成的 Debug APK 安装到 **Android 12（API 31）及以上** 设备。

## 权限说明

- **Android 12（API 31–32）**：首次启动会请求 **读取外部存储**（`READ_EXTERNAL_STORAGE`）。
- **Android 13+**：请求 **读取音频、视频媒体**（`READ_MEDIA_AUDIO`、`READ_MEDIA_VIDEO`）及 **通知**（`POST_NOTIFICATIONS`，用于媒体通知栏控制）。

未授权时仅显示说明文案；授权后需先 **选择目录** 再扫描。

## 功能验证清单

1. **选择目录**：在系统界面中选中目标文件夹后，专辑列表应更新。
2. **专辑 / 歌曲列表**：专辑名、歌曲数量、歌曲标题与排序正确。
3. **播放**：点击歌曲进入「正在播放」，可播放 / 暂停、上一首 / 下一首、拖动进度条。
4. **模式**：点击「播放模式」按钮循环切换：顺序 → 列表循环 → 单曲循环 → 随机；顺序播放到最后一首后停止；列表循环接回第一首；单曲循环在**自然播放结束**时重复当前曲；随机为洗牌队列，播完一轮后重新洗牌。
5. **后台与息屏**：按 Home 或锁屏，声音应继续；下拉通知栏应出现媒体控制（依赖系统与厂商策略）。
6. **持久化**：退出 App 再进入，应能恢复上次专辑/曲目/模式/进度（文件仍存在且目录授权仍有效时）。

## 注意事项

- **无网络权限**：本应用未声明 `INTERNET`。
- **损坏文件**：无法解码的媒体会尽量跳过或报错后切下一首，不应导致进程崩溃。
- **进程被杀**：前台播放可降低被杀概率；极端情况下进度以 DataStore 为准尽力恢复。
- **若换目录**：重新「选择目录」后，会按新目录扫描；旧路径下的播放记录可能无法匹配。
